git push --all &&
  git push --all github &&
  git push --all lantsang-github &&
  git push --all lantsang-gitee &&
  git push --tags &&
  git push --tags github &&
  git push --tags lantsang-github &&
  git push --tags lantsang-gitee
